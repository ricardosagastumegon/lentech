# ============================================================
# MONDEGA DIGITAL — CD Staging
#
# NOTE: Deployment is handled automatically by Railway.
# Railway listens to pushes on `main` and deploys using
# nixpacks (no Dockerfile needed).
#
# This workflow is intentionally disabled until a
# self-hosted staging environment with Docker is set up.
# ============================================================

name: CD — Staging (disabled)

on:
  workflow_dispatch:   # Manual trigger only — not automatic

jobs:
  placeholder:
    name: Deploy placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Deployments are handled by Railway."
          echo "Push to main → Railway auto-deploys apps/web and apps/admin."
          echo "See https://railway.app for deploy status."
