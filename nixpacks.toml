[phases.setup]
nixPkgs = ["nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter admin build"]

[start]
cmd = "pnpm --filter admin start -p ${PORT:-3010}"
